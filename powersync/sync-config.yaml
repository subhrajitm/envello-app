# yaml-language-server: $schema=https://unpkg.com/@powersync/service-sync-rules@latest/schema/sync_rules.json

config:
  edition: 3

streams:
  user_data:
    auto_subscribe: true
    query: SELECT * FROM user_data WHERE user_id = auth.user_id()
